# backup.conf - configuration du backup-lab

# SRC: directory to backup
# DEST: where snapshots are created
# LOGDIR: where logs are written
# KEEP: number of snapshots to keep

SRC="$HOME/backup-lab"
DEST="/backup/storage"
LOGDIR="/var/log/backup"
KEEP=7
